.git
.github
.venv
.claude
.tmp-*
__pycache__
*.py[cod]
.pytest_cache
.ruff_cache
.mypy_cache
.coverage
htmlcov
dist
build
docs/screenshots
docs/*.png
docs/*.jpg
renovate.json5

# Root markdown docs not COPYed in Dockerfile (README.md IS copied — keep it)
CONTRIBUTING.md
GETTING_STARTED.md
CODE_OF_CONDUCT.md
SECURITY.md

# Developer tooling not needed in image
Makefile
.pre-commit-config.yaml

# NOTE: do NOT exclude *.tar — the build context at /tmp/hs-ctx deliberately
# contains hermes_station.tar / extension.tar / tests.tar / docs.tar as
# Apple container CLI 0.12.3 subdirectory-COPY workaround sources. They are
# pre-packed by the build procedure (see CLAUDE.md). The repo root itself
# never contains *.tar files in normal use.
